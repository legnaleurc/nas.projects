services:
  dfd:
    container_name: dfd
    build: .
    ports:
      - "${DFD_HOST}:${DFD_PORT}:80"
    environment:
      DULD_API_URL: "${DULD_API_URL}"
      DULD_API_AUTHORIZATION: "${DULD_API_AUTHORIZATION}"
    restart: on-failure
